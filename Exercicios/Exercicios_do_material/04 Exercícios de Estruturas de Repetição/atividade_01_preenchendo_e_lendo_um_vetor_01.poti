# Preenchendo e Lendo um Vetor I
# Construa um algoritmo que receberá 10 valores inteiros e escreva, um abaixo do outro, os valores 
# na ordem inversa em que foram lidos, ou seja, o último valor lido será o primeiro valor impresso.

var numero := vetor[10]

para i de 0 até 9 faça
	numero[i] := leia_inteiro
fim

para i de 9 até 0 passo -1 faça
	escreva "{numero[i]}"
fim