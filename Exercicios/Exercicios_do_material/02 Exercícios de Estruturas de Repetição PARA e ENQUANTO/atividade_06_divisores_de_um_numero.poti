# Divisores de um número
# Construa um programa que leia um número inteiro e que apresenta todos 
# os seus divisores a partir do número 1.

# Exemplo: se o número fornecido for 10, o programa imprimirá, 
# um abaixo do outro (um por linha), os números: 1, 2 e 5.

var numero := leia_numero

para i de 1 até (numero-1) passo 1 faça
	se (numero mod i) == 0 faça
    	escreva "{i}"
	fim
fim